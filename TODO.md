# TODO - Fix code not running

- [x] Inspect existing HTML pages (index.html, works.html) and identify which elements are present.
- [x] Inspect `js/script.js` and identify assumptions (page1/page2, lightbox elements).
- [ ] Make `js/script.js` resilient so it doesn’t break when elements are missing.
- [ ] Ensure pagination `showPage()` works on all pages that call it.
- [ ] Ensure lightbox caption lookup won’t crash if DOM structure differs.
- [x] Validate by opening `index.html` and clicking pagination buttons + works lightbox.


